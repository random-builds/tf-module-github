name: Terraform check
on:
  workflow_dispatch:
  push:
    paths:
      - '**.tf'

permissions: read-all

jobs:
  build:
    permissions:
      contents: read
    uses: random-builds/gh-workflows/.github/workflows/terraform-check.yaml@ac72f5360047f2af1b2f884e713a069c09f7d8a3
    with:
      terraform_version: 1.11.4
